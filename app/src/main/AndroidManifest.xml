<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sempatpanick.githubuserlookup">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <permission
        android:name="com.sempatpanick.githubuserlookup.READ_DATABASE"
        android:protectionLevel="normal" />
    <permission
        android:name="com.sempatpanick.githubuserlookup.WRITE_DATABASE"
        android:protectionLevel="normal" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@drawable/github"
        android:label="@string/app_name"
        android:roundIcon="@drawable/github"
        android:supportsRtl="true"
        android:theme="@style/Theme.GithubUserLookup"
        android:usesCleartextTraffic="true">
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".receiver.AlarmReceiver"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".SettingsActivity" />
        <activity android:name=".FavoriteActivity" />
        <activity android:name=".DetailUserActivity" />
        <activity android:name=".MainActivity" />

        <provider
            android:name=".provider.UserProvider"
            android:authorities="com.sempatpanick.githubuserlookup"
            android:exported="true"
            android:readPermission="com.sempatpanick.githubuserlookup.READ_DATABASE"
            android:writePermission="com.sempatpanick.githubuserlookup.WRITE_DATABASE" />
    </application>

</manifest>